# 📚 プロジェクト概要と学習ガイド

## プロジェクト構造

```
improved/
├── README.md                 # メインドキュメント（機能説明）
├── ARCHITECTURE.md           # 詳細設計書
├── BUILD_GUIDE.md            # ビルドとテスト実行ガイド
│
├── types.h                   # 型定義（イミュータブルな型）
│
├── core.h                    # コア業務ロジック宣言
├── core.c                    # コア業務ロジック実装（純粋関数）
│
├── io.h                      # I/O抽象化層宣言
├── io.c                      # I/O実装（プラットフォーム依存）
│
├── application.h             # アプリケーション層宣言
├── application.c             # アプリケーション層実装
│
├── extensions.h              # 拡張機能の例
├── extensions.c              # 拡張機能の実装例
│
├── main.c                    # エントリーポイント
├── test_core.c               # ユニットテスト
│
└── CMakeLists.txt            # CMakビルド設定
```

## ファイル機能説明

### 3階層アーキテクチャ

```
Application Layer (application.c/h)
    ↑↓ 依存
Domain Layer (core.c/h)
    ↑↓ 依存
I/O Layer (io.c/h)
    
共通: types.h（型定義）
拡張: extensions.c/h（追加機能の例）
```

## 各ファイルの詳細

### types.h - 型定義（120行）
**目的**: イミュータブルなデータ型を定義

**含まれる内容**:
- Enumeration（PowerLevel, MicrowaveState など）
- Immutable Struct（CookingConfig, MicrowaveStatus など）
- Event型（イベント駆動設計用）
- Result型（エラーハンドリング）

**特徴**:
- 副作用なし
- テスト不要（型チェックのみ）
- 他に依存なし

### core.h/c - ビジネスロジック（~250行）
**目的**: 電子レンジの業務ロジックを純粋関数として実装

**主要関数**:
```
apply_event()              - 状態遷移（FSM）
calculate_remaining_time() - 時間計算
get_magnetron_cycle()      - PWMパターン計算
validate_cooking_config()  - 入力検証
status_to_display()        - 変換
create_initial_state()     - ファクトリ
```

**特徴**:
- **100% 純粋関数** - 副作用なし
- **決定論的** - 同じ入力 = 同じ出力
- **テスト容易** - 外部依存なし
- **キャッシュ可能** - メモ化対応

**テスト対象**:
- すべての関数は個別にテスト可能
- test_core.c で 11 個のテストケース

### io.h/c - I/O抽象化層（~300行）
**目的**: プラットフォーム依存のI/O処理を抽象化

**インターフェース**:
```
DisplayInterface    - 画面表示
InputInterface      - キーボード入力
HardwareInterface   - リレー/マグネトロン制御
TimerInterface      - タイマー機能
```

**実装例**:
- Console（Windows/Unix共通）
- Mock（テスト用）
- 拡張可能（LCD, Web, MQTT など）

**特徴**:
- **プラットフォーム検出** (#ifdef _WIN32)
- **複数バックエンド対応** - 実装の切り替え可能
- **テストフレンドリー** - モック実装が容易

### application.h/c - アプリケーション層（~150行）
**目的**: core と io を統合し、イベントループを実装

**主要機能**:
```
app_create()              - 初期化
app_run()                 - メインループ
apply_state_transition()  - 副作用適用
render_ui()               - UI更新
handle_user_input()       - 入力変換
```

**特徴**:
- **イベントループ** - リアクティブパターン
- **副作用の一元化** - io 呼び出しはここのみ
- **タイマーティック** - 1秒ごと

### main.c - エントリーポイント（~10行）
**目的**: プログラムの開始と終了

**処理**:
```
app_create()  → 初期化
app_run()     → メインループ
app_destroy() → クリーンアップ
```

### test_core.c - ユニットテスト（~250行）
**目的**: core.c の関数をテスト

**テストケース** (11個):
1. calculate_remaining_time
2. next_power_level
3. next_cooking_time
4. get_magnetron_cycle
5. validate_cooking_config
6. apply_event - START_COOKING
7. apply_event - TIMER_TICK
8. apply_event - TIMER_COMPLETE
9. apply_event - PAUSE_RESUME
10. create_initial_state
11. status_to_display

**実行方法**:
```bash
gcc -o test_core test_core.c core.c -std=c99
./test_core
```

### extensions.h/c - 拡張機能（~200行）
**目的**: システム拡張の例を示す

**実装例**:
- Auto Cooking（食材別レシピ）
- Defrost Mode（解凍モード）
- Sensor Integration（センサー統合）
- Energy Saving（省エネモード）
- Statistics（統計情報）

**特徴**:
- すべて **純粋関数**
- **既存コードに影響なし**
- **そのまま実装可能**

### CMakeLists.txt - ビルド設定
**目的**: マルチプラットフォーム対応ビルド

**対応環境**:
- Windows (Visual Studio, MinGW)
- Linux/Unix (GCC, Clang)
- macOS

**ビルドターゲット**:
- microwave（メイン実行ファイル）
- test_core（テスト実行ファイル）

## 設計パラダイムの組み合わせ

### 1. 関数型プログラミング

```c
// 純粋関数の例
uint32_t calculate_remaining_time(uint32_t current, uint32_t tick) {
    return current > tick ? current - tick : 0;
}

// 不変性の例
typedef struct {
    const MicrowaveState state;
    const uint32_t remaining_time;
} MicrowaveStatus;

// 関数合成の例
MicrowaveStatus new_state = apply_event(old_state, event);
new_state = apply_energy_saving(new_state, mode);
```

### 2. イベント駆動設計

```c
enum EventType {
    EVENT_START_COOKING,
    EVENT_TIMER_TICK,
    EVENT_PAUSE,
    // ...
};

// イベント処理
Event event = {.type = EVENT_START_COOKING};
MicrowaveStatus next = apply_event(current, event);
```

### 3. 有限状態機械（FSM）

```
STANDBY ──[START]──> COOKING ──[TICK]──> COMPLETED
  ↑                      ↓
  └────────[STOP]────────┘
           [PAUSE]
             ↓
           PAUSED ──[RESUME]──> COOKING
```

### 4. 依存注入（DI）

```c
typedef struct {
    DisplayInterface display;
    InputInterface input;
    HardwareInterface hardware;
    TimerInterface timer;
} IOContext;

// 実装を切り替え
IOContext io_prod = create_io_context();
IOContext io_test = {
    .display = create_test_display(),
    .input = create_test_input(),
    // ...
};
```

### 5. ストラテジーパターン

```c
typedef struct {
    void (*clear_screen)(void);
    void (*print_line)(const char*, ...);
} DisplayInterface;

// 複数の戦略を実装
DisplayInterface console = create_console_display();
DisplayInterface lcd = create_lcd_display();
DisplayInterface web = create_web_display();
```

## 設計原則の実装マッピング

| SOLID 原則 | 実装箇所 | 例 |
|-----------|--------|-----|
| Single Responsibility | types.h | 各型が単一責務 |
| | core.c | 状態遷移ロジックのみ |
| | io.c | I/O処理のみ |
| Open/Closed | extensions.h | 既存コード変更なく拡張 |
| | io.h | インターフェース拡張 |
| Liskov Substitution | io.h | インターフェース置換可能 |
| Interface Segregation | DisplayInterface, InputInterface | 細粒度なインターフェース |
| Dependency Inversion | IOContext | 抽象に依存 |
| | application.c | 具体実装に依存しない |

## コード品質指標

### テスト性（Testability）
```
✓ 純粋関数 - 外部依存なし
✓ イミュータブル - 隠れた状態変更なし
✓ 結合度低 - 単体テスト可能
✓ テストケース数 - 11個（core.c用）
```

### 保守性（Maintainability）
```
✓ 明確な責務分離 - 各ファイルは1つのレイヤー
✓ 命名規則 - 一貫性のある命名
✓ コメント - 各関数に説明
✓ 複雑度 - 単純な関数が多い
```

### 再利用性（Reusability）
```
✓ 無依存な純粋関数 - どのプロジェクトでも使用可能
✓ インターフェース抽象化 - バックエンド切り替え可能
✓ extensions.h - 拡張メカニズム示唆
```

### 移植性（Portability）
```
✓ C99 標準 - ほぼすべての環境
✓ プラットフォーム検出 - #ifdef で自動判定
✓ 標準ライブラリのみ - 依存なし
✓ CMake - マルチプラットフォーム対応
```

## 学習フロー

### Step 1: データ型を理解
```
1. types.h を読む
2. 各型の目的を理解
3. イミュータビリティを確認
```

### Step 2: コア業務ロジックを理解
```
1. core.h の関数シグネチャを読む
2. core.c の実装を読む
3. 純粋関数の性質を確認
4. test_core.c でテストケースを確認
```

### Step 3: I/O抽象化を理解
```
1. io.h のインターフェースを読む
2. io.c の複数実装を読む
3. プラットフォーム検出メカニズムを理解
```

### Step 4: 統合層を理解
```
1. application.h を読む
2. application.c のイベントループを読む
3. 副作用の処理方法を理解
```

### Step 5: 実装を試す
```
1. テストを実行：./test_core
2. プログラムを実行：./microwave
3. 操作してみる（s,p,r,c,t,w,q キー）
```

### Step 6: 拡張してみる
```
1. extensions.h の例を参考に
2. 新機能を追加
3. テストを追加
```

## チートシート

### よく使う関数

| 関数 | 目的 | 使用場所 |
|------|------|--------|
| `apply_event()` | 状態遷移 | core.c のハート |
| `create_initial_state()` | 初期化 | application.c |
| `status_to_display()` | UI生成 | application.c |
| `handle_user_input()` | 入力解析 | application.c |
| `validate_cooking_config()` | 入力検証 | application.c |

### 追加したい時

| 機能 | 変更範囲 | 難易度 |
|------|--------|--------|
| 新イベント | types.h, core.c | ⭐ |
| 新出力形式 | io.c のみ | ⭐ |
| 新状態 | types.h, core.c | ⭐⭐ |
| 新プラットフォーム | io.c のみ | ⭐⭐ |
| 新業務ロジック | core.c | ⭐⭐ |

## トレンド技術の采用状況

| 技術 | 采用度 | 実装箇所 |
|------|--------|--------|
| 関数型プログラミング | ⭐⭐⭐⭐⭐ | core.c |
| イベント駆動設計 | ⭐⭐⭐⭐⭐ | application.c |
| 有限状態機械 | ⭐⭐⭐⭐⭐ | core.c |
| 依存注入 | ⭐⭐⭐⭐ | io.c, application.c |
| ストラテジーパターン | ⭐⭐⭐⭐ | io.c |
| イミュータブル設計 | ⭐⭐⭐⭐ | types.h |

## 参考資料

### 設計パターン
- [SOLID原則](https://en.wikipedia.org/wiki/SOLID)
- [Design Patterns in C](https://en.wikipedia.org/wiki/Design_pattern)
- [Functional Programming](https://en.wikipedia.org/wiki/Functional_programming)

### C言語
- [C99 Standard](https://en.cppreference.com/w/c/language)
- [POSIX API](https://pubs.opengroup.org/onlinepubs/9699919799/)
- [Best Practices](https://cert.org/secure-coding/)

### ツール
- [CMake Tutorial](https://cmake.org/cmake/help/latest/guide/tutorial/index.html)
- [GDB Reference](https://sourceware.org/gdb/documentation/)
- [Valgrind](https://valgrind.org/docs/manual/)

## まとめ

このプロジェクトは以下を示しています：

✅ **現代的な設計原則** を C で実装
✅ **品質の高いコード** を書くためのパターン
✅ **拡張性** と **保守性** のバランス
✅ **関数型パラダイム** の実践的活用
✅ **テスト駆動設計** の重要性

これらをマスターすることで、Cを使った高品質なシステムソフトウェア開発ができるようになります。
